<div class="col-md-6 col-md-offset-3"></div>
<div class="transitions-enabled" id="pics"></div>
<% @pics.each do |pic|  %>
<div class="box panel panel-default"></div>

<%= link_to (image_tag pic.image.url(:medium)),pic %>

<h2><%= link_to pic.title, pic %></h2>
<%end%>
